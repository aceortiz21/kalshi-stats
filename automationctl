#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${repo_dir}/src${PYTHONPATH:+:${PYTHONPATH}}"
cd -- "${repo_dir}"
exec python3 -m kalshi_stats.telegram_controller_cli "$@" --repository "${repo_dir}"
